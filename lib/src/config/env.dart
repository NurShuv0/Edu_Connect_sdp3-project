class Env {
  /// Base URL of your Node.js backend (without trailing slash).
  /// For Windows development, use 127.0.0.1 instead of localhost
  static const String apiBase = 'http://127.0.0.1:5000/api';
  // If you run Android emulator, use: 'http://10.0.2.2:3000/api'
}
